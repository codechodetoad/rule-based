# Candidate linking (source/eval/rag_link.py, build_library_embeddings.py) calls an
# OpenAI-compatible embeddings endpoint. Copy to .env and fill in your key.
# ViettelAI Model-as-a-Service (Qwen3-Embedding-4B). Self-host an equivalent 4B embedder
# for a fully offline / competition-compliant run.
VIETTELAI_API_KEY=your_key_here
